
//const <id> = <exp>;
//var <id>: <tipus>;

const patata = 2 * (2+3)/(1*5-4/4-3);
const hola = FALS AND CERT;
CONST A = CERT or fals and cert;
const aux = 5;
const cad = "ASDF";
var x: logic;
var y: vector[0..15] de logic;
var z: vector[3..patata] de sencer;
var integer: sencer;

funcio suma(perval asdf : sencer, perref qwer : sencer, perval a : sencer): sencer;
	var resultat: SENCER;
	const patata = 0;
	func
		integer = qwer + 3;
		si cert llavors
		    retornar 3;
        sino
            si fals llavors
                retornar 4;
            fisi
            retornar 1;
        fisi;

	fifunc;

prog
	integer = suma(0, integer, 2);
	integer = 4+z[aux]*43+4;
	escriure(z[4]);
	cicle
		llegir(x);
	fins integer < 3+56;

	escriure(cad);

	mentre cert fer
		llegir(x);
	fimentre;

	si 34 > 3*2-14/7 llavors
		x = fals;
	fisi;

fiprog