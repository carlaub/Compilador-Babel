
//const <id> = <exp>;
//var <id>: <tipus>;

const patata = 2 * (2+3)/(1*5-4/4-3);
const hola = FALS AND CERT;
CONST A = CERT or fals and cert;
var integer: sencer;

funcio suma(perref asdf : sencer, perval qwer : logic): SENCER;
	var resultat: SENCER;
	const patata = 0;
	func
		retornar 0;
	fifunc;

prog
	a = suma;
	retornar CERT OR CERT AND FALS;
fiprog