
//const <id> = <exp>;
//var <id>: <tipus>;

const patata = 2 * (2+3)/(1*5-4/4-3);
const hola = FALS AND CERT;
CONST A = CERT or fals and cert;
var x: logic;
var y: vector[0..15] de sencer;
var z: vector[3..5] de sencer;
var integer: sencer;

funcio suma(perval asdf : sencer, perref qwer : sencer, perval a : sencer): SENCER;
	var resultat: SENCER;
	const patata = 0;
	func
		retornar 0;
	fifunc;

prog
	a = suma(0, integer, 2);
	z[3] = z[5];
	retornar CERT OR CERT AND FALS;
fiprog