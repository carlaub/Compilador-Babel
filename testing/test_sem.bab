
//const <id> = <exp>;
//var <id>: <tipus>;

const patata = 2 * (2+3)/(1*5-4/4-3);
const hola = FALS AND CERT;
CONST A = CERT or fals and cert;
var x: logic;
var y: sencer;
var integer: sencer;

funcio suma(perref asdf : sencer, perref qwer : sencer, perref asdf : logic, perval a : sencer): SENCER;
	var resultat: SENCER;
	const patata = 0;
	func
		retornar 0;
	fifunc;

prog
	a = suma(x , y, 2);

	retornar CERT OR CERT AND FALS;
fiprog