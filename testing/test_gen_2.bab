var integer1 : sencer;
var integer2 : sencer;

funcio random(perval a: sencer): sencer;
	var x : sencer;
func
	x = 4;
	escriure(a);
	si a == 0 llavors
		retornar 1;
	sino
		retornar a * random(a-1);
	fisi;
fifunc;

prog
    //integer1 = 1;
    //integer2 = 2;

    integer1 = random(4);

    //escriure("integer1: ", integer1);

    //cicle
        escriure("volta: ", integer1);
        //integer1 = integer1 + 1;
    //fins integer1 > 4;


fiprog