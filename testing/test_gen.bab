const CTE1 = "F2P1.bab";
CONST CTE2 = ((3 - 1) * 4) / 2;
var v4:logic ;
const MIN = -50;
const MAX = 50;
CoNsT CTE4 = cert AND cert;
var  v1:sencer ;
var integer : sencer;
var result : sencer;
var sortir : logic;

VaR v5: vector [MIN .. MAX] de sencer;
var v7:logic ;

prog
    integer = 8;
	result = integer+v1;
    result = integer/2;
    escriure(result);
    result = 9
    escriure(result);
    sortir = cert;
    escriure (sortir);
    sortir = fals;
    escriure(sortir);
	v7 = v4;
fiprog