const CTE1 = "F2P1.bab";
//CONST CTE2 = ((3 - 1) * 4) / 2;
var v4:logic ;
//const MIN = -50;
//const MAX = 50;
//CoNsT CTE4 = cert AND cert;
var  v1:sencer ;
var integer1 : sencer;
var integer2 : sencer;
var hola : sencer;
var var_logic1 : logic;
var var_logic2 : logic;
var result : sencer;
var result_logic : logic;
var v: vector [0 .. 100] de sencer;

funcio random(perval a: sencer, perval b: sencer): sencer;
	var x : logic;
func
	a = 4;
	retornar a+2;
fifunc;

funcio pardal(perval a: sencer, perval b: sencer): sencer;
	var y : logic;
func
	a = 4;
	retornar 2;
fifunc;

prog
	integer1 = random(1, result) + 3 / pardal(1, result);
    integer1 = 4;
    result = integer1+2*3;
    //escriure("Num2: ", v[integer1+3]);
    escriure("Escriu: ");
    llegir(hola);
    v[hola] = result;
    escriure("Esto es hola = ", v[hola]);
    cicle
		mentre cert fer
			si var_logic1 llavors		//Si aquest condicional està més amunt, salta una excepció
				integer1 = 3;	//També si la seva condició és una variable, però això crec que ve relacionat per la condició del mentre
			fisi;				//He afegit un parche i un to-do per a revisar-lo
		fimentre;
	fins var_logic1;
fiprog